Remediation versions of the scripts found here: https://blueprint.asd.gov.au/configuration/intune/devices/scripts/
Each script will what options to set when adding into Intune.

There is also a version of everything rolled into the one script, to save filling your scripts page with noise.
One pair for user-context, and another for device context.
